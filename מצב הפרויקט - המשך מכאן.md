# מצב הפרויקט - המשך מכאן (Handoff)

> ## 🔧 סשן 8.7.2026 (המשך) - סבב תיקונים 2 (5 סעיפים גדולים)
> 1. **עמוד הצמצום קוצר והודגש:** במקום פסקה ארוכה - "חסרות לך X שעות" גדול + שתי נקודות מודגשות ("נושא שתסמני יורד מהתוכנית"; "לא חייבת לצמצם - הכול יידחס"). נושא מסומן מקבל מסגרת אדומה + תג "יורד מהתוכנית שלך".
> 2. **פופ-אפ אחרי צמצום:** הכפתור המשני הוא עכשיו "👀 להסתכל קודם על התוכנית שהתקבלה" (מסגרת teal בולטת) שסוגר וגולל לראש התוכנית.
> 3. **⭐ תתי-נושאי חובה בגאנט/PDF/יומן:** חולצו מילה-במילה מ-mifrat-7th/8th/9th (364 פריטים, אומתו כ-substring מול ה-docx, 0 כשלים) ל-`app/src/data/coreSubtopics.ts`. כל שיעור מציג עכשיו קודם ליבה (תג "חובה" teal) ואז הרחבה/רשות. גם ב-ICS ובתיאור אירועי היומן. ראה זיכרון core-subtopics-extracted - **לא לחלץ שוב.** (grade7/8/9.ts + modelTasks.ts לא שונו - שיוך משימות המודל של ח' נשמר.)
> 4. **⭐ חקר ט' שוכתב:** במקום "שבועות שלמים", עכשיו **שיעור חקר אחד בשבוע** ביום ההוראה האחרון: 15.10-15.11 כל שבועיים, 15.11 עד הגשת הפוסטרים (15.2) כל שבוע. שאר השיעורים תוכן, שנדחס (גם למורה עם מעט שעות - "חייב להקצות זמן לחקר גם אם דוחסים"). בלוק STEM (שבוע שלם לפני 1.12) נשאר.
> 4ב. **אבני הדרך = תזכורות, לא שיעורים:** הגשות/ירידים (18.1/1.2/15.2/16.3/2.5/1.6) לא תופסות שיעור - צ'יפ "תזכורת" מקווקו בשורת השבוע, וביומן/ICS "תזכורת: ..." בצבע נפרד. רק שיעורי החקר המוקצים הם שעת לימוד (ירוק, kind 'חקר').
> 5. **כניסת הרכזת הובלטה:** כרטיס מובחן במסך הפתיחה ("רכזת מדע וטכנולוגיה? כל תוכניות הצוות במקום אחד") במקום קישור טקסט קטן.
>
> **✅ delivery.gs v13 פורסמה ואומתה (8.7 23:05):** תיאור אירועי היומן (תתי-נושא) + צבע תזכורות. `DELIVERY_URL` עודכן ל-`AKfycbyeNDQHXrhiTIuNjFeES_rr3296Aftn9Rj7zeJf3LJa4LYwxyoDv2wQwfy8iM4OaiVEDQ`. **הכפתור הצף למשוב/תקלות הוגדל (70px) עם תווית קבועה.** הכול חי ונדחף.


> ## 🔧 סשן 8.7.2026 - סבב תיקונים של נופר (8 סעיפים, הכול בקוד)
> 1. **ניסוחי המחסור משקפים בלבד** - הוסרו כל ההמלצות "לצמצם עוד" (כלל מחייב של נופר: משקפים, לא ממליצים ולא מטים).
> 2. **PDF מילים מחוברות - תוקן מהשורש:** באג ידוע של html2canvas בעברית (RTL) - במסלול רינדור-לפי-מילה הרווחים נשמטים אצל חלק מהמשתמשות (אומת מול ה-PDF הפגוע של ט'1 ב-Drive: האלמנט התקין היחיד היה זה עם letter-spacing). התיקון: `letter-spacing: 0.15px` על כל הדף במצב `exporting` (index.css) - מכריח רינדור אות-אות ששומר רווחים. **לא להסיר את הכלל הזה.**
> 3. **חקר רק בכיתה ט':** `researchTrack()` רץ רק לט' (plan.ts); המקרא, פרטי בית הספר (יריד חקר) והטקסטים מותאמים לפי שכבה. בז'/ח' אין שום זכר לירידי חקר.
> 4. **כיתה ט' - סידור גושים:** המורה מסדרת 3 גושים (כימיה/ביולוגיה/פיזיקה, לפי תחומי mifrat-9th) ולא 15 נושאים; בתוך גוש - סדר המפרט. הוסרו כל הטענות "כולם חובה" ותגי חובה מ-ט' (לא נכון לפי נופר). `topicOrder` נשאר רשימה שטוחה - המנוע לא השתנה; סדר ישן מנורמל אוטומטית.
> 5. **מיילים מציינים כיתה + מועד עדכון** (נושא + גוף, גם למורה וגם לרכזת) - delivery.gs.
> 6. **מיילים בעדכון חוזר:** הוסבר ותוקן - ב-v11 היומן סונכרן לפני המיילים; עדכון חוזר (מחיקת ~193 אירועים + הוספת ~193) חצה את מגבלת 6 הדקות של Apps Script והריצה נקטעה לפני המיילים. עכשיו: המיילים נשלחים **לפני** היומן, והסנכרון הפך ל**סנכרון-הפרש** (Events.list אחד, מוסיפים רק חדשים ומוחקים רק מה שירד) - מהיר בסדר גודל.
> 7. **כפילות יומנים:** היומן מזוהה לפי מפתח קבוע בתיאור היומן (`plan-key:סמל|כיתה|מייל`) ולא לפי שם; שם משתנה -> אותו יומן מתעדכן. כפילויות באותו שם/מפתח נמחקות אוטומטית. ⚠️ היומן הכפול הישן של ט'2 (שנוצר לפני v11, בשם בלי כיתה) - לבקש מנופר למחוק ידנית בחשבון ההתיישבותי.
> 8. **תצוגת רכזת מאורגנת:** מורה -> שורת כיתה -> "גאנט PDF" + "גאנט יומן" + תאריך עדכון; חיפוש לפי שם מורה כשיש יותר מ-4 מורות.
>
> 9. **מסך הכניסה זוכר פרטים קודמים** (בקשת נופר): כניסה מוצלחת נשמרת בדפדפן; בפעם הבאה השדות מולאו מראש, שם בית הספר נשלף מיד, ובהקלדה מוצעות השלמות (datalist + autocomplete). גם בכניסת רכזת.
>
> 10. **כפתור משוב ודיווח תקלות (צף, בסגנון הלוגו) - ✅ חי באתר:** כפתור עגול עם הלוגו בפינה השמאלית-תחתונה, נפתח לבחירה בין "דיווח על תקלה" ל"משוב על המערכת"; 5 שאלות כלליות משותפות (ממולאות מראש מהסשן), ואז שאלות פר מסלול (משוב: 13 שאלות בקבוצות, הכול חובה מלבד "הערות נוספות", כולל שאלה מותנית; תקלה: תיאור + מכשיר + דפדפן, ו-userAgent נשלח אוטומטית). נשלח ל-`apps-script/feedback.gs` **v2 בחשבון האישי של נופר (gergrood)** - גיליון "משוב ודיווח תקלות - תומכת הוראה אישית" בדרייב האישי + מייל התראה על כל תקלה. `FEEDBACK_URL` ב-export.ts. אומת חי (גיליון + מייל). גם כאן נדרשה מלכודת ההרשאות: `authorizeNow` -> אישור -> New deployment.
> 11. **תמונת שיתוף (OG) + אנליטיקס:** `og-image.jpg` חדש 1200x630 ב-63KB (הישן היה PNG של 1.6MB - כבד מדי לתצוגה מקדימה בוואטסאפ) + תגיות og:image מלאות; הותקן Google Analytics (GA4, `G-Q1TDLMD8B5`) ב-index.html. שניהם אומתו חי.
>
> **✅ v12 פורסמה ואומתה (8.7.2026 17:41).** נופר פרסמה New deployment; `DELIVERY_URL` עודכן ב-export.ts. אומת בבדיקת POST ישיר (בלי מיילים/קבצים): התשובה מכילה את השדות החדשים (`added/kept/removed/deduped`), ובקשה חוזרת עם אירוע שהשתנה החזירה `kept=2, added=1, removed=1` על **אותו** יומן (id זהה) - סנכרון-ההפרש וזיהוי-לפי-מפתח עובדים חי.

> ## 🌐 האתר חי! (6.7.2026)
> - **כתובת למורות:** https://smart-plan-mada-vetech.vercel.app
> - **GitHub:** https://github.com/nofarg8/smart-plan-mada-vetech - **כל `git push` מעדכן את האתר אוטומטית** (Vercel, Root Directory=app).
> - **כפתור הדיבאג מופיע רק ב-localhost** (`DEBUG_ENTRY = import.meta.env.DEV`) - באתר החי נופר נכנסת בזרימה הרגילה (סמל 999999 + המייל שלה כרכזת).
> - **נוסף בסשני 6-7.7:** לוגו + favicon + OG; שאלון 3 שלבים עם פס התקדמות (מורה חוזרת נוחתת תמיד על מסך השעות); **מצב פר-כיתה** (בחירת כיתה מרשימה נגללת ז'1-ט'5, חובה; כל כיתה = שעות/צמצומים/עריכות/קבצים/יומן/שורת-חריגות משלה; דריסה רק בעדכון אותה כיתה - אומת מול v11); **כיתה ט' מלאה** (grade9.ts מ-mifrat-9th, 15 נושאים=125ש', בלי לומדות מודל, "סדרי את הנושאים" עם חיצים, שבועות חקר מאמצע נובמבר עד יריד 16.3 + שבוע תחרויות STEM לפני ההגשה המחוזית 1.12); אירועי בית ספר; עריכת שיעור ידנית; שיעור היכרות/סיכום קבועים; נעילת נושאים גדולים שרובם חובה מצמצום; כפתור "עריכת הלו"ז" פותח את פאנל הנושאים; תצוגת רכזת (listPlans); הודעות שגיאה מפנות לסטטוס תשפ"ז; **PDF תוקן מהשורש** (הזרקת CSS לעותק המצולם - מרוץ טעינה גרם לתקלות לסירוגין; לא להשתמש ב-zoom ב-CSS).
> - **⚠️ לתחילת הסשן הבא: לנופר יש רשימת תיקונים חדשה** - לשאול אותה מהם לפני הכול.

> נקודת הכניסה לכל סשן חדש. מתאר **מה נבנה, איפה הכול, מה נשאר, ומאיפה להמשיך.**
> עודכן לאחר סשן שבו: חובר הסטטוס החי, נבנתה הזרימה המלאה (הזדהות → לו״ז אישי), הושלמו משימות המודל לז'+ח', והמנוע עבר לפריסה שבועית לפי שעות המורה.
>
> ## ⭐⭐ עדכון הסשן האחרון - פאזת המסירה נבנתה ועובדת חי (הכי חשוב) ⭐⭐
> **פאזת המסירה (פאזה ב') הושלמה ואומתה חי מקצה לקצה.** בלחיצה אחת על הכפתור **"הורידי PDF וקבלי גוגל קלנדר"** (במסך התוצאה):
> 1. **PDF** של הגאנט (מלא, עם כל תתי-הנושא) יורד למחשב המורה.
> 2. נשלח לסקריפט **`apps-script/delivery.gs`** (רץ בחשבון ההתיישבותי, Web App) שעושה: מתייק PDF+ics ב**תת-תיקייה לכל בית ספר** (`שם - סמל`) בתוך תיקיית האם ב-Drive; **יוצר יומן Google אישי למורה, ממלא אותו בכל אירועי התוכנית (~193), ומשתף אותו עם המורה** (מופיע לבד, בלי ייבוא, ומתעדכן לבד בכל מסירה חוזרת); שולח **עותק קלנדר לרכז/ת במייל**.
> 3. המורה רואה רק **"נשלח לרכז/ת שלך"** - התיוק הפנימי בדרייב שלנו **מוסתר** ממנה (הוראת נופר).
>
> **בחירת אופציה ב' (יומן משותף) על פני ייבוא ידני:** ייבוא ה-.ics אצל נופר נכשל (מסך הייבוא של גוגל החזיר "לא הצלחנו לטעון" - תקלת ריבוי-חשבונות בצד גוגל, לא הקובץ). לכן עברנו ליומן שההתיישבותי יוצר ומשתף.
>
> **מה שלמדנו על הרשאות Apps Script (קריטי לכל שינוי עתידי ל-`delivery.gs`):** כשמוסיפים קוד שנוגע ב-CalendarApp צריך **גם** להוסיף את השירות המתקדם (Services → Google Calendar API) **וגם** לאשר את הרשאת היומן - וזה **לא** נתפס אוטומטית. הפתרון שעבד: מוסיפים פונקציה `authorizeNow(){ CalendarApp.getAllCalendars(); }`, מריצים אותה בעורך, **מאשרים את מסך ההרשאות** (כולל יומן), **ואז מפרסמים New deployment** (הפרסום "נועל" את ההרשאות שהיו קיימות בזמנו - צריך פרסום חדש אחרי האישור). ה-"New version" לא נשמר לנופר -> עשינו New deployment וקיבלנו URL חדש.
> **`DELIVERY_URL`** (הכתובת הפעילה ב-`app/src/export.ts`): `https://script.google.com/macros/s/AKfycbwH11DVmPOxv3VBG5C7qbmYrCsIUfK6Y831GbblZy9U6IaRl3M8bX72u6AaxTAm11O76w/exec` (גרסה 12, 8.7.26 - מיילים עם כיתה+מועד לפני היומן, סנכרון-הפרש, יומן לפי מפתח; אומת ב-POST ישיר). תיקיית האם ב-Drive: `1r0I5ZDkI5VzBgfhKB91VOI71VzMkTQOI`. **הערה: אצל נופר "New version" לא עובד - תמיד New deployment -> URL חדש -> לעדכן כאן וב-export.ts.**
>
> **⚠️ לבדוק בסשן הבא:** נופר מתקנת טעות הקלדה במייל שלה בסטטוס עבור 999999 (`gergroood` עם 3 o -> `gergrood`). בגלל הטעות, גם היומן וגם מייל הרכז נשלחו לכתובת שגויה. **לוודא אחרי התיקון:** שהיומן המשותף **מופיע** בגוגל קלנדר של נופר (תחת "יומנים אחרים"), ושמייל הרכז מגיע. (בדיבאג, מייל המורה **וגם** הרכז נשאבים שניהם מאותה שורת סטטוס, לכן טעות אחת פגעה בשניהם; בזרימה אמיתית המורה מקלידה מייל משלה.)
>
> ## עדכון סשן קודם (מחסור + חגים + גאנט מקיף + העשרה):
> - **מנגנון מחסור השעות** (`AdjustPanel`): בחירת נושאים לצמצום + כפתור אישור + מד חי + נימוק פדגוגי + תיוג חובה/הרחבה/רשות. אחרי אישור **החלק נסגר** לסרגל סיכום ("פתח לעריכה"). **שורת הכוונה חיה** ("אם תצמצמי עוד X - זמן מלא; אם לא - דחיסה"). **צמצום חלקי** -> סרגל אזהרה אדום (לא "הכול תקין" מטעה).
> - **דחיסה במקום השמטה:** במחסור, כל הנושאים נשארים (הקצב לא מוגבל -> דחיסה פרופורציונלית), לא נשמטים נושאי סוף-השנה. `shortfallHours` מחושב מ-capacity מול planned.
> - **חגים רשמיים** מ-`data/holidays.ts` (משרד החינוך): שיעור שנופל עליו חג = "אין שיעור".
> - **גאנט מקיף:** כל הנושאים לאורך השבועות (בלי דגימה) + תתי-נושא (הרחבה/רשות) בכפתור "הצג תתי-נושא" מצומצם, + כפתור גלובלי "הרחיבי/סגרי את כל תתי-הנושאים".
> - **העשרה לפי הסטטוס:** יוזמות/תחרויות מתויגות **"מזכירים"** (אם צוינו בטקסט היוזמות שבסטטוס, לפי `keywords`) או **"הצעה"**; + **ימים מדעיים בינלאומיים** (מעמודת `scienceDays` בגאנט) כהצעות עוגן לנושא.
> - **כפתור דיבאג** במסך הפתיחה מתחבר לסטטוס האמיתי (סמל **999999**, "נופר דיבאג"), מאחורי `DEBUG_ENTRY` (לכבות לפני פרסום). **הוסר** ה-fallback המזויף (קריאת סטטוס שנכשלת מחזירה null, לא בית ספר דמו).

## מה בונים (במשפט)
כלי ווב בשם **"תומכת הוראה אישית"** למורי מדע וטכנולוגיה בחט"ב (המחוז ההתיישבותי, תשפ"ז): המורה מזדהה, מזינה את **שעות ההוראה בפועל** שלה, והמערכת בונה **פריסה שבועית אישית מתוארכת** (1.9.26 - 20.6.27) - שיעור אחר שיעור, עם נושאים, משימות מודל, חגים, יוזמות וחקר משובצים - לייצוא כ-PDF וכקלנדר. **בלי LLM** - מנוע חוקים דטרמיניסטי. **כיתות ז' ו-ח'** (ט' מתועד, לא נבנה).

## ⭐ מטרת-העל (אל תשכח את זה)
לא "טבלה". המורה מזינה **מתי היא מלמדת** (ב' 2ש', ג' 1ש', ה' 2ש'), ומקבלת את **השיעורים שלה בפועל** ממולאים אוטומטית - מה ללמד בכל שיעור, ומתי עושים מודל/חקר/מבחן. הערך = האוטומציה האישית (מפרט התכנים + מיפוי המודל + הגאנט החי + הסטטוס החי + שעות המורה). ראה memory [[core-value-personal-schedule]], [[result-is-weekly-planner]].

## 🎯 מטרות התוכנה והתוצרים הסופיים (למה כל זה קיים)
**המטרה הסופית: לייצר לכל מורה גאנט/לו״ז אישי משלה, ולתייק אותו אוטומטית.** התוכנה לוקחת את המקורות הרשמיים + שעות המורה ומפיקה, לכל מורה:

1. **גאנט אישי כ-PDF** - הפריסה השבועית שלה, שיעור אחר שיעור, לכל השנה. זה **התוצר המרכזי** - "גאנט משל עצמה".
2. **קלנדר אישי (Google Calendar)** - אותה תוכנית כאירועים מתוארכים.
3. **עותק של הקלנדר נשלח לרכז/ת** של אותו בית ספר (המייל מהסטטוס).
4. **תיוק אוטומטי בחשבון Google Drive של ההתיישבותי (שלנו):**
   - **תיקייה לכל בית ספר**, לפי שם בית הספר.
   - בתוך תיקיית בית הספר - **כל המורים של אותו בית ספר**, ולכל אחד/ת **הגאנט (PDF) והקלנדר שלו/ה**.
   - כלומר: מספר מורים באותו בית ספר -> כולם מתוייקים תחת אותה תיקיית בית ספר. הרכז/ת רואה במקום אחד את כל התוכניות של הצוות שלו/ה.

**סטטוס: שלבים 1-4 של המסירה - ✅ בנויים ועובדים חי** (דרך `apps-script/delivery.gs`, בלי שרת נפרד - Apps Script בחשבון ההתיישבותי). ההבדל היחיד מהתיאור המקורי: הקלנדר של המורה **אינו** מיוצא כקובץ לייבוא, אלא **יומן שההתיישבותי יוצר ומשתף** עם המורה (מופיע ומתעדכן לבד). ראה [[delivery-outputs]], [[full-user-flow]], והסעיף "פאזת המסירה" למטה.

## מצב נוכחי - הזרימה המלאה עובדת, חי

| רכיב | מצב | איפה |
|---|---|---|
| **נתונים** (נושאים, שעות, אופציונלי) | ✅ שלם, מהמקור הרשמי | `app/src/data/grade7.ts`, `grade8.ts` |
| **משימות מודל** | ✅ ז'=29, ח'=18 (מלא, ממופה לנושאי המפרט) | `app/src/data/modelTasks.ts` |
| **מנוע** | ✅ פריסה שבועית אישית + חודשי + חקר רשמי | `app/src/engine/plan.ts` |
| **גאנט חי** | ✅ נקרא חי מ-Google Sheets (CSV+CORS) | `app/src/data/liveGantt.ts` |
| **סטטוס חי** | ✅ **מחובר ועובד** (CSV מפורסם מסונן) | `app/src/data/statusApi.ts` |
| **שפת עיצוב** | ✅ Rubik + צבעי handoff + לוגואים + שם | `app/src/index.css`, `index.html` |
| **הזדהות + זרימה** | ✅ מסך פתיחה → אימות מול סטטוס → לו״ז | `app/src/App.tsx` |
| **מנגנון מחסור שעות** | ✅ `AdjustPanel` - צמצום נושאים + אישור + מד + נימוק פדגוגי | `app/src/App.tsx`, `engine/plan.ts` |
| **חגים רשמיים (יום הוראה)** | ✅ מרשימת משרד החינוך; שיעור בחג = "אין שיעור" | `app/src/data/holidays.ts`, `engine/plan.ts` |
| **גאנט מקיף + תתי-נושא** | ✅ כל הנושאים לאורך השבועות + הרחבה/רשות תחת כל נושא (כפתור הצג/הסתר + גלובלי) | `engine/plan.ts` (`buildWeeklySchedule`) |
| **העשרה: מזכירים/הצעה + ימים מדעיים** | ✅ יוזמות מתויגות לפי טקסט הסטטוס (`keywords`); ימים מדעיים מהגאנט כהצעות | `data/initiatives.ts`, `engine/plan.ts`, `App.tsx` (`isRegistered`) |
| **מסירה: PDF + יומן משותף + תיוק Drive + מייל לרכז** | ✅ בנוי ואומת חי (כפתור אחד) | `app/src/export.ts`, `apps-script/delivery.gs`, `App.tsx` |
| **מסכי שאלון מלאים (עיצוב 1-4)** | ⬜ אופציונלי (הזרימה הנוכחית היא wizard מינימלי) | - |
| **תצוגת רכז (6)** | ⬜ עתידי | - |

הרצה: `npm run dev --prefix app`. **בפועל השרת עולה על `http://localhost:5173` או `5174`** (וייט בוחר פורט פנוי). **תצוגה מקדימה: בכרום**, לא בפאנל המוטמע.

## הזרימה שעובדת עכשיו (מקצה לקצה, חי)
1. **מסך פתיחה / הזדהות** (`LoginScreen` ב-`App.tsx`): שם, אימייל, סמל מוסד, אימייל רכז. **אימות:** שולף מהסטטוס החי לפי סמל מוסד; אם אימייל הרכז תואם -> נכנס.
   - **כניסת דיבאג:** כפתור "כניסת דיבאג" (או סמל מוסד `DB`) מתחבר לסטטוס האמיתי של נופר (סמל **999999** - "נופר דיבאג", ז'=11/ח'=12, יוזמות "עין העדשה"). מאחורי `DEBUG_ENTRY` ב-`App.tsx` - **לכבות ל-false לפני פרסום.**
2. **פאנל "פרטי בית הספר"** (`SchoolInfo`): מציג מה שנשלף מהסטטוס - ש"ש לשכבה, מעבדה, יריד. **לא שואלים את המורה מה שכבר בסטטוס.**
3. **מערכת שעות** (`ScheduleEditor`): מולאה מראש מש"ש הסטטוס; המורה מחלקת לימים. `weeklyHours` = הסכום.
4. **פריסה שבועית אישית** (`WeeklyPlan`/`WeekRow`): לכל שבוע, השיעורים של המורה (ב'/ג'/ה'...) ממולאים: נושא ללימוד, ומתי בדיוק משימת מודל (עם סוג+פירוט+תג "אירוע הערכה"), חקר או מבחן. + מבט חודשי קומפקטי למעלה.

## המנוע (`app/src/engine/plan.ts`) - הפונקציות
- **`buildPlan(input)`** -> `Plan`. `input = { grade, weeklyHours, modelTaskDays? }`.
- **`scheduleTopics`** - פורס נושאים על **כל השנה** לפי **קצב** (`pace = סך שעות / סך מקדמי שבועות`, מוגבל ל-weeklyContent). לא דוחס לתחילת השנה -> תוכן בכל חודש ספט-יוני.
- **`scheduleModelTasks`** - משבץ משימות מודל **לפי הנושא המשויך** (`topicNames`), מפוזרות לשבוע הפחות-עמוס (לא נערמות). fallback: חודש -> מחרוזת.
- **`researchTrack`** - אבני דרך חקר בתאריכים **הרשמיים**: 18.1 הגשת נושא, 1.2/15.2 פוסטרים, **יריד מחוזי 16.3**, 2.5 הגשה לכנס ארצי, **יריד ארצי 1.6**.
- **`buildWeeklySchedule(plan, teacherSlots)`** -> `WeekSchedule[]` - **הלב.** לכל שבוע לימוד: משבצות ההוראה של המורה, מלאות בנושא + משימות מודל/חקר/מבחן בשיעורים המתאימים (עם `taskType`, `detail`, `isAssessment`). **מקיף:** מחלק את *כל* הנושאים הפעילים על השיעורים (בלי לדלג), ומצרף `topicList` + `subItems` (הרחבה/רשות). **חג:** שיעור שנופל עליו חג רשמי (`officialHolidayOn`) מסומן `kind:'חג'` ולא מקבל תוכן.
- **`droppedTopics` (ב-`EngineInput`)** - נושאים שהמורה בחרה לצמצם במחסור; יורדים מהפריסה. `plan.capacityHours/plannedCoreHours/shortfallHours` מזינים את `AdjustPanel`.
- **`buildMonthlyCalendar(plan)`** -> מבט חודשי: שעות, נושאים, חגים נקיים, מבחנים, משימות מודל, יוזמות STEM, חקר.
- `buildPlanTable`/`crossYearModelTasks` - קיימים, **לא מוצגים** (הוחלפו בפריסה השבועית). אפשר להסיר בעתיד.

## מקורות חיים (Live) - שניהם מחוברים ✅
1. **גאנט חי** (`liveGantt.ts`): Google Sheet **ציבורי** (`12nXRAYl3l6UgAJUL35OuhJ_UfWrK9ThTD_zVluFwFzE`). ה-CSV export תומך ב-CORS -> הדפדפן מושך ישיר. parse+clean (סינון אירועי צוות, מקפים, בלי "?") -> `GanttWeek[]`. `setLiveGantt()` מחליף בזמן ריצה; `gantt.ts` המוטמע = fallback. המנוע צורך שבוע/חודש/תאריכים/חגים.
2. **סטטוס חי** (`statusApi.ts`): Google Sheet **פרטי** (`1x_t0v1w3GGyQqGomuuusm5rE4pS1y2uQq5hH5EWYCOw`). **הפתרון שעבד:** נופר פרסמה לשונית מסוננת **"API"** (11 עמודות בלבד, נוסחת `=QUERY('תגובות תשפ"ז'!A:CZ,"SELECT C,E,F,H,AG,AH,AI,AJ,AL,AM,AO",1)`) כ-**CSV ציבורי (Publish to web)**. האפליקציה קוראת חי (CORS), מוצאת לפי סמל מוסד, מאמתת מייל רכז. `fetchSchool(semel)`. דמו `mockStatus.ts` = fallback.
   - **התובנה שפתחה את זה:** הארגון (hityash.org) **חוסם Apps Script "Anyone"** (403) אבל **מאשר "Publish to web"**. "לשלוח החוצה" מותר, "למשוך פנימה אנונימית" חסום. אם צריך לחבר עוד גיליון פרטי - **Publish to web CSV, לא Apps Script.** (הקובץ `apps-script/status.gs` נשאר כגיבוי לשיטה החלופית, לא בשימוש.)
   - **פרטיות:** הלשונית המסוננת בלי טלפונים/שדות מנהליים; כן מייל רכז (נחוץ לאימות + קלנדר) -> בקישור ציבורי, בידיעת נופר.

## 📦 פאזת המסירה (בנויה ועובדת - קרא לפני שנוגעים בה)
**זרימה:** כפתור אחד ב-Header, **"הורידי PDF וקבלי גוגל קלנדר"** -> `ResultScreen.doFinalize` -> מוסיף class `exporting` על הדף (מסתיר פקדים, כופה פתיחת כל תתי-הנושא ל-PDF) -> קורא ל-`finalizePlan` ב-`app/src/export.ts`.

**`app/src/export.ts` (כל לוגיקת הייצוא):**
- `generatePdfBase64(element)` - יוצר PDF מהדף עם **html2pdf.js** (הותקן: `npm i html2pdf.js`; יש `src/html2pdf.d.ts` להצהרת טיפוס). מחזיר base64.
- `buildICS(plan, session, weekly, stamp)` - בונה מחרוזת .ics תקינה (RFC 5545, כולל קיפול שורות ל-Hebrew). `planEvents(...)` מחזיר את אותם אירועים כרשימת `{start,end,title}` (תאריכים מקומיים YYYY-MM-DD) - **זה מה שנשלח לסקריפט לבניית היומן.**
- **תוכן הקלנדר (~193 אירועים):** אירוע לכל **יום הוראה** עם מה שמלמדים (מ-`weekly` slots, kind 'נושא', תאריך `sl.dateISO`), + משימות מודל/חקר/מבחנים (מ-`plan.deadlines`), + יוזמות, + חופשות רשמיות. `SlotAssignment.dateISO` מחושב במנוע ב-`buildWeeklySchedule` (יום שלא נופל בשבוע חלקי -> null -> לא נכנס, מונע כפילות בשבוע הראשון).
- `finalizePlan(...)` - מייצר PDF, מוריד אותו למורה, ו-`postDelivery` שולח POST ל-`DELIVERY_URL` (Content-Type text/plain כדי לעקוף preflight, כמו בסטטוס). ה-payload: `{action:'deliverPlan', schoolId, schoolName, teacherName, teacherEmail, coordinatorEmail, calendarName, pdfBase64, icsContent, events}`.
- `DeliverResult.calendarShared` - נגזר מ-`data.calendar && !data.calendarError`; ה-UI מציג הודעה כנה (אם היומן נכשל -> "היומן עדיין לא הופעל", לא "שותף").

**`apps-script/delivery.gs` (רץ בחשבון ההתיישבותי, Web App):**
- `doPost` -> `deliverPlan_`: תת-תיקייה `שם ביה"ס - סמל` בתוך `DELIVERY_PARENT_ID` -> שומר PDF + .ics (דורס לפי שם). `syncCalendar_` יוצר/מוצא יומן לפי שם, **מנקה אירועים בטווח השנה ובונה מחדש** (עדכון בלי כפילות), ומשתף (`shareCalendar_` -> `Calendar.Acl.insert` reader) עם המורה + הרכז. `MailApp` שולח .ics לרכז.
- **דורש:** שירות מתקדם "Google Calendar API" (Services) + הרשאת יומן מאושרת (ראה "מה שלמדנו על הרשאות" למעלה).
- כל חלק ב-try/catch -> תקלה ביומן/מייל לא חוסמת את תיוק ה-PDF.

**מגבלות/פתוחים במסירה (ראה גם "פתוחים"):**
- **מייל למורה מהסטטוס בדיבאג:** בכניסת דיבאג `teacherEmail = school.coordinatorEmail` (שניהם מהסטטוס) - לכן טעות במייל בסטטוס פגעה גם ביומן וגם ברכז. בזרימה אמיתית המורה מקלידה מייל בעצמה.
- **ריבוי יומנים:** נוצר יומן לכל מורה בחשבון ההתיישבותי - עם מאות מורים יצטבר. לשקול.
- **זמן:** יצירת ~193 אירועי יומן לוקחת ~40-80 שניות ("מפיק..." ארוך). תקין, אפשר לייעל בעתיד.
- **PDF לרכז:** כרגע הרכז מקבל **רק .ics** במייל; ה-PDF אצלו דרך תיקיית ה-Drive. נופר ביקשה לשקול לצרף PDF גם למייל (שינוי קטן ב-`delivery.gs` + פרסום מחדש).

## הנתונים (מספרים מאומתים מהמקור הרשמי)
- כיתה ז': **109 ש'** (מכסה 110). כיתה ח': **138 ש'** (מכסה 140). ש"ש: ז'=5, ח'=6.
- **משימות מודל ז' = 29** (כימיה/פיזיקה עם שמות אמיתיים ממסמך moodle; ביולוגיה לפי נושא+סוג מהתמונה של נופר). **ח' = 18** (לפי התמונה, ממופה לנושאי mifrat-8th).
- **ייצוג משימות ללא שם בודד:** ביולוגיה ז' וכל ח' נבנו כ**נושא+סוג** (התמונה נתנה ספירה, לא שמות) עם פירוט רשמי גנרי (`LOMDA`/`ASSESS` ב-`modelTasks.ts`). כשיהיו שמות אמיתיים - להחליף.

## מפת קבצים
**קוד (`app/src/`):**
- `data/types.ts` - מודל: `GradeBank`, `BreadthTopic`, `ModelTask` (כולל `topicNames`, `classification`, `detail`, `isAssessment`), `GanttWeek`, `InitiativeEvent`, `SchoolStatus`, `TeacherSlot`.
- `data/grade7.ts`, `grade8.ts` - בנק תכנים (מ-mifrat-7th/8th).
- `data/modelTasks.ts` - משימות מודל ז'(29)+ח'(18).
- `data/gantt.ts` - גאנט מוטמע (fallback) + `setLiveGantt()`.
- `data/liveGantt.ts` - קריאת גאנט חי (CSV).
- `data/initiatives.ts` - יוזמות STEM/תחרויות (מהגאנט).
- `data/holidays.ts` - **חופשות/חגים רשמיים תשפ"ז** (הודעת משרד החינוך). המנוע בודק אם יום הוראה נופל בחג. ראה [[official-holidays-source]].
- `data/statusApi.ts` - **חיבור הסטטוס החי** (`fetchSchool`).
- `data/mockStatus.ts` - סטטוס דמו (fallback).
- `engine/plan.ts` - **המנוע.**
- `App.tsx` - כל ה-UI (Login, SchoolInfo, ScheduleEditor, WeeklyPlan, MonthCard, AdjustPanel, ResultScreen). כולל `DEBUG_ENTRY` וכפתור המסירה.
- `export.ts` - **כל לוגיקת המסירה** (PDF, .ics, `finalizePlan`, `DELIVERY_URL`). ראה סעיף פאזת המסירה.
- `html2pdf.d.ts` - הצהרת טיפוס ל-html2pdf.js.
- `index.css` - כל העיצוב (כולל `@media print` ו-`.exporting` ל-PDF).

**עיצוב (handoff):** `design_handoff_annual_plan/מסכי תוכנית עבודה.dc.html` + `README.md` + `assets/logo-*.png`. **המלצה בלבד** - הוראות נופר גוברות.
**מקורות רשמיים:** `מפרט התכנים.../mifrat-7th (1).docx`, `mifrat-8th.docx`, `mifrat-9th.docx`; xlsx גאנט; מסמך moodle של משימות המודל; התמונה של נופר עם ספירת המשימות.
**apps-script/status.gs** - סקריפט קריאת סטטוס חלופי (לא בשימוש; הסטטוס עובד דרך Publish-to-web CSV).
**apps-script/delivery.gs** - ⭐ **סקריפט המסירה הפעיל** (Web App בחשבון ההתיישבותי): תיקיות + PDF + יומן משותף + מייל לרכז. הקוד בקובץ = הגרסה הפרוסה. מקור-האמת לקוד ה-Apps Script של המסירה.
**קוד מקור הסטטוס המלא של נופר (הפניה):** github.com/nofarg8/STATUS-MADATECH - מערכת הסטטוס (טופס+גיליון+Apps Script `savePdfSummary_` שהראה שאפשר POST ל-Apps Script ולשמור PDF ב-Drive - הבסיס לדפוס המסירה שלנו).

## החלטות וכללים מחייבים (קריטי - אל תפר)
1. **מקורות רשמיים בלבד** - נתונים מה-docx/xlsx/גיליונות של משרד החינוך, **לא** מהאפיון. אסור להמציא שמות/שעות/תאריכים.
2. **מקפים: רק מקף מקלדת `-`** בטקסט גלוי. בלי `–` / `—`.
3. **בלי סימני "?"** בתוכן הגלוי למורה.
4. **בלי טקסט פיתוח/דמו/placeholder ב-UI** ("בפיתוח", "דמו", "מסונכרן עם...", credentials לדוגמה, hints). ראה [[no-dev-text-in-ui]] - נופר תיקנה על זה שוב ושוב.
5. **ניסוח למשתמש: "פרטי בית הספר"**, לעולם לא "סטטוס".
6. **אסור לשאול את המורה שאלה שהתשובה כבר בסטטוס** - רק להציג לאישור.
7. **חודש STEM (ינואר) = ארצי** (המחוז מריץ עבורו פעילויות/תחרויות). **ירידי חקר: מחוזי 16.3 ואז ארצי 1.6** (מתקבלים למחוזי, עולים לארצי). ראה [[status-user-facing-wording]].
8. **לסנן אירועי צוות ההדרכה** (וובינרים, השתלמויות, כנסי צוות/רכזים).
9. **מסירה:** PDF + קלנדר למורה; הקלנדר גם לרכז; תיקיית Drive לכל בית ספר בחשבון ההתיישבותי. ראה [[delivery-outputs]], [[full-user-flow]].
10. **עדכון מסמכים: רק `.md`** בתיקייה, לא `.docx`.
11. **הוראות בעל-פה של נופר גוברות על ה-handoff העיצובי.**

## ⏳ פתוחים (חלקם ממתינים לנופר - להזכיר לפני "הכול מוכן")
1. **סיווג חובה/רשות של משימות המודל** - נופר תפרט ידנית. אין מקור בתיקייה, אסור להמציא. השדה `ModelTask.classification` מוכן; ה-UI יציג תג ברגע שימולא. ראה [[pending-model-task-classification]].
2. **שמות בודדים למשימות ביולוגיה ז' + כל ח'** - כרגע נושא+סוג. להחליף כשיהיו.
3. **מסכי שאלון מלאים בעיצוב** (1-4, פס התקדמות) - הזרימה הנוכחית היא wizard מינימלי פונקציונלי; אם רוצים את חוויית העיצוב המלאה, זו הרחבה.
4. ~~**מסירה**~~ **בוצע ואומת חי** (PDF + יומן משותף + Drive + מייל לרכז, דרך `delivery.gs`). ראה סעיף "פאזת המסירה". **מה שנשאר לבדוק/לשקול:** (א) ⚠️ **לוודא אחרי שנופר תיקנה את מייל 999999** (`gergroood`->`gergrood`) שהיומן מופיע אצלה ושמייל הרכז מגיע; (ב) לצרף PDF גם למייל לרכז (אם נופר תרצה); (ג) ריבוי יומנים בחשבון ההתיישבותי (מורה=יומן); (ד) זמן יצירת יומן (~40-80ש').
5. **כיתה ט'** - ראה spike למטה. מחייב אישור נופר.
6. ~~**מנגנון מחסור שעות**~~ **בוצע** - `AdjustPanel` (בחירת נושאים + אישור + מד + נימוק). ראה [[shortage-adjust-mechanism]].
7. השלמת `gantt.ts` המוטמע לשלמות (מינורי - הגאנט החי מכסה).
8. **חילוץ תתי-נושאי הליבה מהמפרט** - כרגע הגאנט מציג תחת כל נושא רק את פריטי ה**הרחבה/רשות** (מחולצים נקי). המפרט קובע שעות ברמת **נושא** בלבד (לתתי-נושא אין שעות), ושמות תתי-הליבה הם טקסט תוכנית מפורט שקשה לחלץ נקי אוטומטית (מודגש ≠ היררכיה). אם רוצים גם את שמות תתי-הליבה תחת כל נושא - צריך חילוץ ידני מוקפד מ-`mifrat-7th/8th.docx` (לא אוטומטי, לא להמציא).
9. **נושא בודד בשבוע עמוס** - אם *כל* שיעורי השבוע נתפסים ע"י משימות מודל/חג, נושא שפעיל רק באותו שבוע לא יופיע בפריסה השבועית (מופיע במבט החודשי). קצה מינורי (למשל "הגוף כמערכת על" בז').

## לקחים מהסשן הזה (חשוב לסשן הבא)
- **הערך = הפריסה האישית לפי שעות המורה, לא טבלאות.** נופר התסכלה כשקיבלה טבלאות. תמיד לחשוב "מה המורה צריכה לעשות בשיעור הזה".
- **בלי טקסט פיתוח ב-UI. לעולם.** לסרוק לפני סיום.
- **חיבור גיליון פרטי = Publish to web CSV** (לא Apps Script, שנחסם בארגון). לשונית מסוננת עם QUERY לפרטיות.
- **הגאנט + הסטטוס חיים** - לא להטמיע כעותק. הם מתעדכנים.
- כשנופר נותנת מקור (PDF/תמונה/גיליון) - **לקרוא אותו** (pypdf זמין; docx/xlsx = zip של XML; תמונה = לקרוא ישירות). לא לנחש.
- כשמקורות רשמיים סותרים - **להציף לנופר**, לא להכריע לבד.
- **פאזת המסירה - לקחים:**
  - נופר **לא-טכנולוגית ומתלחצת מצעדים טכניים** - להסביר בפשטות מוחלטת, "מי עושה מה", ולהדריך צעד-צעד. לא להציף בז'רגון (base64/URL/ספריות).
  - **POST ל-Apps Script Web App כן עובד בארגון** (בניגוד למה שחשבנו על הסטטוס - שם החסימה הייתה על doGet אנונימי; המסירה עובדת דרך doPost). הדפוס: PDF נוצר בצד-לקוח, נשלח base64, הסקריפט שומר - בדיוק כמו `savePdfSummary_` של הסטטוס.
  - **הרשאות יומן ב-Apps Script הן מלכודת:** צריך Services→Calendar API + הרצת פונקציה שנוגעת ב-CalendarApp לאישור + New deployment אחרי האישור. "no permissions popped up" בהרצת doGet = doGet לא נוגע ביומן; צריך `authorizeNow` שכן נוגע.
  - **בעיות "לא הגיע" הן כמעט תמיד נתונים, לא קוד** - טעות o כפול במייל בסטטוס פגעה גם ביומן וגם ברכז. תמיד לבדוק את הנתון בסטטוס לפני שמחפשים באג.
  - **ריבוי חשבונות Google** אצל נופר גרם לתקלות (מסך ייבוא נכשל, /u/0/). לשים לב.
  - הורדת PDF כ-base64 בצד-לקוח = **html2pdf.js** (רסטר של הדף; עברית/RTL מושלמים כי זה צילום). מצב `.exporting` כופה פתיחת תתי-נושא + הסתרת פקדים.

## כיתה ט' - spike (לא לבנות בלי אישור)
מבנה תוכן זהה לז'/ח' (140 ש', עץ נושאים עם שעות) -> `grade9.ts` כ-`GradeBank` רגיל. ההבדל: יש **נושאים שלמים** הרחבה/רשות ואין סדר הוראה כפוי -> המורה בוחרת אילו לכלול, סדר גמיש. **ווים ארכיטקטוניים:** (1) בורר שכבה יתמוך בכרטיס ט'; (2) `EngineInput.mode`: ז'/ח'=פריסה-אוטומטית, ט'=נושאים-נבחרים+סדר-מוצע; wizard של ט' יוסיף שלב בחירת נושאים.

## סביבת פיתוח
Node v24.13.1, npm 11.8.0, Git 2.53.0. React 19 + Vite 8 + TS 6 (מקומי ל-`app/`, 0 גלובלי). הרצה: `npm run dev --prefix app`. בנייה: `npm run build --prefix app`. pypdf זמין ל-PDF.

---

## 🚀 פרומפט פתיחה לסשן הבא (להעתיק ל-Claude Code)

```
שלום! ממשיכים את הפרויקט "תומכת הוראה אישית" - כלי ווב לבניית תוכנית עבודה
שנתית במדע וטכנולוגיה (חט"ב ז'-ח', המחוז ההתיישבותי, תשפ"ז).

קרא קודם, בסדר הזה:
1. "מצב הפרויקט - המשך מכאן.md" (בשורש) - מסמך המצב המלא. קרא אותו במלואו,
   ובמיוחד את הבלוק "עדכון הסשן האחרון - פאזת המסירה" ואת סעיף "פאזת המסירה".
2. CLAUDE.md ו-README - כללי עבודה והיקף.
3. הזיכרון (memory) - במיוחד: core-value-personal-schedule, no-dev-text-in-ui,
   what-is-the-status, result-is-weekly-planner, delivery-outputs, delivery-apps-script.

מצב נוכחי (הכל עובד חי): הזדהות מול סטטוס חי → פרטי בית הספר → מערכת שעות
אישית → פריסה שבועית מקיפה (שיעור-אחר-שיעור, כל הנושאים + תתי-נושא, משימות
מודל, חגים, יוזמות/הצעות, ימים מדעיים) → מנגנון מחסור/דחיסה → **ופאזת המסירה
בלחיצה אחת: PDF מלא יורד למורה, יומן Google אישי נוצר ומשותף איתה (מופיע
ומתעדכן לבד), הכל מתויק בתיקיית ביה"ס ב-Drive של ההתיישבותי, ועותק נשלח
לרכז/ת. (דרך apps-script/delivery.gs - Web App בחשבון ההתיישבותי.)

⚠️ הדבר הראשון לעשות: **לוודא שהמסירה עדיין עובדת, ובמיוחד שהיומן המשותף
נוצר ומופיע.** תיקנתי טעות הקלדה במייל שלי בסטטוס (999999: gergroood→gergrood).
הרץ את המסירה בכניסת דיבאג (כפתור "כניסת דיבאג") ובדוק ש-calendarShared=true
(אין calendarError בתגובה מ-delivery.gs), ותשאל אותי אם היומן באמת הופיע לי
בגוגל קלנדר ואם מייל הרכז הגיע. אם היומן נכשל - קרא ב-handoff את "מה שלמדנו
על הרשאות Apps Script".

כללים קריטיים: מקורות רשמיים בלבד (בלי המצאה); רק מקף מקלדת; בלי "?"; בלי שום
טקסט פיתוח/דמו ב-UI; "פרטי בית הספר" ולא "סטטוס"; אסור לשאול מה שכבר בסטטוס;
המורה לא רואה שמתייק אצלנו ב-Drive (רק "נשלח לרכז"); תצוגה מקדימה בכרום;
בלי LLM (מנוע דטרמיניסטי). הוראות בעל-פה שלי גוברות על העיצוב.

כניסת דיבאג: כפתור "כניסת דיבאג" במסך הפתיחה (סמל 999999 - נופר דיבאג).
השרת: npm run dev --prefix app (פורט 5173 או 5174).

מה אני רוצה לעשות עכשיו: [כאן נופר תכתוב - למשל: לבדוק/לייעל את המסירה /
לצרף PDF למייל לרכז / לסווג חובה/רשות של משימות המודל / מסכי שאלון בעיצוב /
תצוגת רכז / כיתה ט'].

בוא נמשיך.
```
